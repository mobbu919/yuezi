---
/**
 * SeriesCard — A series overview card with a pre-rendered 3x3 mosaic cover.
 *
 * The mosaic cover is generated at build time by scripts/generate-covers.mjs
 * into a SINGLE image at /covers/{slug}.jpg, so the gallery overview page
 * only loads 5 images total instead of 45 thumbnails.
 *
 * Props:
 *   series  — Series object from src/data/series.js
 */

export interface Props {
  series: any;
}

const { series } = Astro.props;
const coverUrl = `/covers/${series.slug}.jpg`;
---

<a href={`/gallery/${series.slug}`} class="series-card" aria-label={`View series: ${series.name}`}>

  <!-- Single pre-rendered mosaic cover -->
  <div class="series-cover">
    <img
      src={coverUrl}
      alt={`${series.name} series cover`}
      class="series-cover-img"
      loading="lazy"
      decoding="async"
    />

    <!-- Hover overlay -->
    <div class="series-overlay" aria-hidden="true">
      <span class="series-open-label">View Series</span>
    </div>
  </div>

  <!-- Card body -->
  <div class="series-body">
    <div class="series-heading">
      <span class="series-order">Series {series.order}</span>
      <span class="series-count">{series.works.length} Works</span>
    </div>
    <h3 class="series-name">{series.name}</h3>
    <p class="series-description">{series.description}</p>
  </div>

</a>

<style>
  .series-card {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  /* ---- Cover ---- */
  .series-cover {
    position: relative;
    overflow: hidden;
    margin-bottom: var(--spacing-md);
    aspect-ratio: 1 / 1;
    background: var(--color-bg-alt);
  }

  .series-cover-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s cubic-bezier(0.25, 0.1, 0.25, 1);
  }

  .series-card:hover .series-cover-img {
    transform: scale(1.03);
  }

  /* Hover overlay */
  .series-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background var(--transition-base);
    z-index: 1;
  }

  .series-open-label {
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    letter-spacing: var(--letter-spacing-wide);
    text-transform: uppercase;
    color: var(--color-white);
    opacity: 0;
    transform: translateY(8px);
    transition: opacity var(--transition-base), transform var(--transition-base);
    background: rgba(0, 0, 0, 0.55);
    padding: var(--spacing-xs) var(--spacing-lg);
  }

  .series-card:hover .series-overlay,
  .series-card:focus-visible .series-overlay {
    background: rgba(0, 0, 0, 0.08);
  }

  .series-card:hover .series-open-label,
  .series-card:focus-visible .series-open-label {
    opacity: 1;
    transform: translateY(0);
  }

  /* ---- Body ---- */
  .series-body {
    padding: 0;
  }

  .series-heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-xs);
  }

  .series-order,
  .series-count {
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    letter-spacing: var(--letter-spacing-wide);
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .series-name {
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-tight);
    color: var(--color-text);
    margin-bottom: var(--spacing-xs);
  }

  .series-description {
    font-size: var(--font-size-sm);
    line-height: var(--line-height-base);
    color: var(--color-text-secondary);
  }
</style>
